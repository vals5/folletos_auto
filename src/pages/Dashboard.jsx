import DashboardLayout from "../components/layout/DashboardLayout";
import { Typography } from "@mui/material";

export default function Dashboard() {
  retrun(
    <DashboardLayout>
      <Typography variant="h4">Bienvenido al sistema de folletos</Typography>
    </DashboardLayout>,
  );
}
